---
phase: 03-直接聊天交互体验
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - main.py
  - tests/test_main_commands.py
autonomous: true
requirements:
  - CHAT-05
must_haves:
  truths:
    - "命令缺参、用法错误、目标不存在时，用户先看到问题说明，再看到 1-2 条可直接照抄的命令示例"
    - "帮助提示保持短、以立即纠错为主，不演变成长篇教程"
  artifacts:
    - path: "main.py"
      provides: "统一的错误/帮助提示构造与命令级示例文案"
      exports:
        [
          "OpenCodePlugin.oc_handler",
          "OpenCodePlugin.oc_mode",
          "OpenCodePlugin.oc_send",
          "OpenCodePlugin.oc_session",
        ]
    - path: "tests/test_main_commands.py"
      provides: "命令误用与缺参帮助回归覆盖"
  key_links:
    - from: "main.py"
      to: "tests/test_main_commands.py"
      via: "命令错误路径输出"
      pattern: "示例:|/oc |/oc-send|/oc-mode|/oc-session"
---

<objective>
把 Phase 3 剩余的帮助体验补齐：命令出错时，不再只报错或贴长说明，而是直接给出下一步可复制的命令。

Purpose: 这一步专门落实 D-10、D-11 和 CHAT-05；Phase 3 的交互体验不仅是“能看到过程”，也包括“走错了能立刻被扶正”。
Output: 统一的短帮助文案、命令误用测试、以及可直接复制的示例命令。
</objective>

<execution_context>
@/home/LiHaoyuan/workspace/cool_stuff/astrbot_plugin_opencode/.opencode/get-shit-done/workflows/execute-plan.md
@/home/LiHaoyuan/workspace/cool_stuff/astrbot_plugin_opencode/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-直接聊天交互体验/03-CONTEXT.md
@main.py
@tests/test_main_commands.py

<interfaces>
From `main.py`:
```python
async def oc_handler(self, event: AstrMessageEvent, message: str = "")
async def oc_mode(self, event: AstrMessageEvent, mode_value: str = "")
async def oc_send(self, event: AstrMessageEvent, path: str = "")
async def oc_session(self, event: AstrMessageEvent, query: str = "")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先把缺参与误用场景的帮助输出写成回归测试</name>
  <files>tests/test_main_commands.py</files>
  <read_first>
    - tests/test_main_commands.py
    - main.py
    - .planning/phases/03-直接聊天交互体验/03-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `/oc` 在没有任务内容时，先说明“未收到任务内容/请输入任务”，再给两条具体示例（D-10, D-11）。
    - Test 2: `/oc-mode` 输入不存在的 mode 时，先说明未找到，再给 ` /oc-mode ` 与一个可复制的设置示例。
    - Test 3: `/oc-session` 查不到目标会话时，先说明问题，再给 ` /oc-session ` 和带编号的绑定示例。
    - Test 4: `/oc-send` 没有可发送文件时，先列出错误，再给查看列表与按编号发送的示例。
  </behavior>
  <action>在 `tests/test_main_commands.py` 为 `/oc`、`/oc-mode`、`/oc-session`、`/oc-send` 的错误分支新增断言，统一要求输出包含“问题说明 + `示例:` + 1-2 条命令示例”。示例内容直接写死为面向当前项目的中文命令，例如 `/oc 帮我总结当前目录结构`、`/oc-mode code`、`/oc-session 2`、`/oc-send 1,2`，不要只断言“包含斜杠命令”这种模糊条件。</action>
  <acceptance_criteria>
    - `tests/test_main_commands.py` 至少包含 4 个命令错误/缺参帮助测试。
    - 每个新增测试都显式断言 `示例:` 与具体命令字符串。
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_main_commands.py -q</automated>
  </verify>
  <done>帮助输出的核心契约被测试固定：错误提示必须帮助用户立即纠错，而不是只报告失败。</done>
</task>

<task type="auto">
  <name>Task 2: 在主命令错误路径中统一输出短帮助与示例</name>
  <files>main.py</files>
  <read_first>
    - main.py
    - tests/test_main_commands.py
    - .planning/phases/03-直接聊天交互体验/03-CONTEXT.md
  </read_first>
  <action>按 D-10、D-11 在 `main.py` 中补一个统一的短帮助构造方式，并改造四个错误路径：`/oc` 空输入时输出 `❌ 未收到任务内容`，并附 `示例:` 下的 `/oc 帮我总结当前目录结构`、`/oc 参考我刚发的图片继续排查`；`/oc-mode` 未找到 mode 时附 `/oc-mode`、`/oc-mode code`；`/oc-session` 未找到目标时附 `/oc-session`、`/oc-session 2`；`/oc-send` 没有有效文件时附 `/oc-send`、`/oc-send 1,2`。文案保持 3-6 行、以立即纠错为目标，不要增加大段教学说明或协议背景。</action>
  <acceptance_criteria>
    - `main.py` 中上述 4 条错误路径都输出 `示例:` 与 1-2 条固定命令。
    - 错误文案长度保持短格式，不出现超过两段的大段说明。
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_main_commands.py -q</automated>
  </verify>
  <done>命令误用时，用户可以直接照抄下一步命令完成纠错，CHAT-05 的帮助体验落地完成。</done>
</task>

</tasks>

<verification>
- `.venv/bin/python -m pytest tests/test_main_commands.py -q`
- 抽查 `/oc`、`/oc-mode`、`/oc-session`、`/oc-send` 错误路径均带 `示例:`
</verification>

<success_criteria>

- 关键命令的错误提示都先说问题，再给可复制示例
- 帮助文案简短，服务立即纠错而不是教学展开
  </success_criteria>

<output>
After completion, create `.planning/phases/03-直接聊天交互体验/03-03-SUMMARY.md`
</output>
