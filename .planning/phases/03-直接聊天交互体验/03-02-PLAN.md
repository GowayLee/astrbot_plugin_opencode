---
phase: 03-直接聊天交互体验
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - main.py
  - core/output.py
  - tests/core/test_output_events.py
  - tests/test_main_commands.py
autonomous: true
requirements:
  - CHAT-03
  - CHAT-04
  - SAFE-02
must_haves:
  truths:
    - "权限确认消息在 IM 中按分行摘要展示，用户能一眼看出操作类型、目标对象和可选项"
    - "用户仍可用纯文本数字或关键词完成授权，超时/拒绝默认失败关闭"
    - "`/oc-new`、`/oc-end`、`/oc-session` 的结果提示收敛成摘要优先，不再每次展开完整默认状态快照"
  artifacts:
    - path: "core/output.py"
      provides: "分行权限确认渲染与标准选项文案"
      exports: ["OutputProcessor._format_permission_update"]
    - path: "main.py"
      provides: "纯文本权限等待与生命周期摘要渲染"
      exports:
        [
          "OpenCodePlugin._wait_for_permission_choice",
          "OpenCodePlugin._render_lifecycle_status",
          "OpenCodePlugin.oc_new",
          "OpenCodePlugin.oc_end",
          "OpenCodePlugin.oc_session",
        ]
    - path: "tests/test_main_commands.py"
      provides: "权限确认与生命周期摘要回归测试"
  key_links:
    - from: "core/output.py"
      to: "main.py"
      via: "`_format_permission_update()` -> `_wait_for_permission_choice()`"
      pattern: "权限确认|选项"
    - from: "main.py"
      to: "tests/test_main_commands.py"
      via: "`oc_new` / `oc_end` / `oc_session` 摘要输出"
      pattern: "工作目录|当前会话|当前 agent|当前 mode"
---

<objective>
把权限确认和会话命令回显收敛成 IM 友好的摘要样式，让用户既能快速读懂，也能继续用纯文本完成控制。

Purpose: 这一步直接落实 D-06、D-07、D-08、D-09，同时覆盖 SAFE-02 和 CHAT-04；Phase 2 已经把生命周期语义做稳了，Phase 3 要做的是让这些稳定语义在聊天里更清楚、更少噪音。
Output: 分行权限确认样式、纯文本授权回归测试、以及 `/oc-new` `/oc-end` `/oc-session` 的摘要优先回显。
</objective>

<execution_context>
@/home/LiHaoyuan/workspace/cool_stuff/astrbot_plugin_opencode/.opencode/get-shit-done/workflows/execute-plan.md
@/home/LiHaoyuan/workspace/cool_stuff/astrbot_plugin_opencode/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-直接聊天交互体验/03-CONTEXT.md
@.planning/phases/02-会话内核与生命周期统一/02-04-SUMMARY.md
@main.py
@core/output.py
@tests/core/test_output_events.py
@tests/test_main_commands.py

<interfaces>
From `main.py`:
```python
def _render_lifecycle_status(self, session, headline: str, *, include_proxy: bool = False, extra_lines: Optional[list[str]] = None) -> str
async def _wait_for_permission_choice(self, event, session) -> tuple[str, bool]
async def oc_new(self, event: AstrMessageEvent, path: str = "")
async def oc_end(self, event: AstrMessageEvent)
async def oc_session(self, event: AstrMessageEvent, query: str = "")
```

From `core/output.py`:

```python
def _format_permission_update(self, permission_payload: dict[str, Any]) -> str
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先固定权限确认与生命周期摘要的聊天契约</name>
  <files>tests/core/test_output_events.py, tests/test_main_commands.py</files>
  <read_first>
    - tests/core/test_output_events.py
    - tests/test_main_commands.py
    - core/output.py
    - main.py
    - .planning/phases/03-直接聊天交互体验/03-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: 权限确认输出为多行文本，至少包含“操作/类型、目标、选项”三块信息，并保留数字编号（D-06, D-07）。
    - Test 2: `_map_permission_reply()` 仍支持数字、标准中文关键词和原始 optionId，超时返回 `cancelled`（D-07, SAFE-02）。
    - Test 3: `/oc-new`、`/oc-end`、`/oc-session` 默认输出只保留 headline、工作目录、当前会话、当前 agent、当前 mode；默认 agent/default mode/proxy 等非关键状态不再每次铺开（D-08, D-09）。
  </behavior>
  <action>在 `tests/core/test_output_events.py` 新增权限确认多行排版断言，明确检查换行、`1.`/`2.` 编号和目标路径；在 `tests/test_main_commands.py` 新增 `/oc-end`、`/oc-session`、`/oc-new` 摘要输出断言，要求结果里保留 `工作目录`、`当前会话`、`当前 agent`、`当前 mode`，同时不再出现 `默认 agent`、`默认 mode`、`代理环境` 这些常规噪音字段；补上 `_map_permission_reply()` 的数字/中文关键词/超时回归断言。</action>
  <acceptance_criteria>
    - `tests/core/test_output_events.py` 包含权限确认多行输出断言，且断言 `\n` 分行存在。
    - `tests/test_main_commands.py` 对生命周期命令显式断言保留字段与省略字段。
    - `tests/test_main_commands.py` 或相邻测试文件包含 `_map_permission_reply()` 的数字/关键词映射断言。
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/core/test_output_events.py tests/test_main_commands.py -q</automated>
  </verify>
  <done>权限确认和生命周期摘要的聊天合同先被测试钉死，后续实现改坏时会立刻回归失败。</done>
</task>

<task type="auto">
  <name>Task 2: 实现分行权限确认与摘要优先生命周期回显</name>
  <files>main.py, core/output.py</files>
  <read_first>
    - main.py
    - core/output.py
    - tests/core/test_output_events.py
    - tests/test_main_commands.py
    - .planning/phases/03-直接聊天交互体验/03-CONTEXT.md
  </read_first>
  <action>按 D-06~D-09 改造文案和渲染：在 `core/output.py:_format_permission_update()` 中输出固定四段式多行文本：第一行 `⚠️ 权限确认`，第二行 `操作: {toolName} ({toolKind})`，第三行 `目标: {detail}`（无 detail 时写 `目标: 未提供`），第四行起逐行列出 `1. 允许一次` / `2. 拒绝` 这类编号选项；在 `main.py` 中保留 `_wait_for_permission_choice()` 的数字和关键词回复逻辑，超时统一走 `cancelled` 并输出“已因超时取消本次授权请求”；同时把 `_render_lifecycle_status()` 和 `oc_session()` 列表/绑定成功/失败文案收敛为摘要优先，只默认展示 `工作目录`、`当前会话`、`当前 agent`、`当前 mode`，不要再自动展开默认 agent/default mode/proxy 环境，除非当前分支确实需要强调该信息。</action>
  <acceptance_criteria>
    - `core/output.py` 中权限确认字符串包含 `操作:`、`目标:`、编号选项，并使用换行拼接而不是 `|` 横向堆叠。
    - `main.py` 的生命周期摘要默认不再调用 `include_defaults=True` 的完整快照模式。
    - `main.py` 中权限超时提示统一为“已因超时取消本次授权请求”或等价固定中文文案。
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/core/test_output_events.py tests/test_main_commands.py -q</automated>
  </verify>
  <done>用户在 IM 中能更自然地完成授权和会话控制：确认消息易读、回复方式仍是纯文本、生命周期命令结果简洁但关键状态完整。</done>
</task>

</tasks>

<verification>
- `.venv/bin/python -m pytest tests/core/test_output_events.py tests/test_main_commands.py -q`
- 检查权限确认文案为分行结构，生命周期回显为摘要优先结构
</verification>

<success_criteria>

- 权限确认在手机 IM 中可直接阅读
- 纯文本数字/关键词授权继续可用，超时与拒绝保持 fail-closed
- `/oc-new`、`/oc-end`、`/oc-session` 默认只展示关键状态
  </success_criteria>

<output>
After completion, create `.planning/phases/03-直接聊天交互体验/03-02-SUMMARY.md`
</output>
