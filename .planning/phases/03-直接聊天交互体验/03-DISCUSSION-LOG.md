# Phase 3: 直接聊天交互体验 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-03-30
**Phase:** 03-直接聊天交互体验
**Areas discussed:** 过程播报密度, 权限确认呈现, 会话命令回显, 错误与帮助提示, 基础回传链路

---

## 过程播报密度

| Option   | Description                                                  | Selected |
| -------- | ------------------------------------------------------------ | -------- |
| 关键节点 | 只发已接收/开始、关键计划变化、权限确认、完成/失败等关键消息 |          |
| 工具级流 | 工具开始/更新也持续发，尽量贴近真实 agent 过程               |          |
| 极简     | 只保留开始、权限确认、最终结果                               |          |
| 用户修正 | 关键节点为主，同时加入极简工具流信息                         | ✓        |

**User's choice:** 关键节点为主，并补充极简工具流信息。
**Notes:** 不要完整工具级刷屏，但也不能只有最终结果。

---

## 权限确认呈现

| Option   | Description                                             | Selected |
| -------- | ------------------------------------------------------- | -------- |
| 分行说明 | 操作类型、目标对象、可选项分 2-3 行展示，继续纯文本确认 | ✓        |
| 单行紧凑 | 保持当前一行摘要 + 选项                                 |          |
| 强警示版 | 进一步强调风险原因和默认超时拒绝                        |          |

**User's choice:** 分行说明。
**Notes:** 继续保留纯文本编号/关键词确认方式。

---

## 会话命令回显

| Option   | Description                                | Selected |
| -------- | ------------------------------------------ | -------- |
| 摘要优先 | 结果明确后只展示少量关键状态               | ✓        |
| 完整快照 | 继续展开目录、默认偏好、会话 ID 等完整状态 |          |
| 极简确认 | 仅保留极短成功/失败提示                    |          |

**User's choice:** 摘要优先。
**Notes:** 工作目录、当前会话绑定、当前 agent/mode 足够；不必每次都完整展开默认偏好。

---

## 错误与帮助提示

| Option     | Description                            | Selected |
| ---------- | -------------------------------------- | -------- |
| 错误+示例  | 先说错在哪里，再给可直接照抄的命令示例 | ✓        |
| 解释型帮助 | 给更多背景和参数解释                   |          |
| 极简错误   | 只报错，不带示例                       |          |

**User's choice:** 错误 + 示例。
**Notes:** 帮助提示应优先让用户下一条就能改对。

---

## 基础回传链路

| Option         | Description                                          | Selected |
| -------------- | ---------------------------------------------------- | -------- |
| 节流式正文回传 | 将 `message_chunk` 打通到 AstrBot，并做合并/节流展示 | ✓        |
| 只打通骨架     | 先接通正文 chunk，但展示先保持朴素                   |          |
| 仍等最终结果   | 继续只展示结构化进度和最终结果                       |          |

**User's choice:** 节流式正文回传。
**Notes:** 用户进一步强调当前项目还不能把 ACP 服务器返回的对话信息传回 AstrBot，Phase 3 必须先修好这条基础能力。

---

## the agent's Discretion

- 正文节流/合并的具体算法
- 极简工具流中保留哪些事件
- 分行权限确认的具体文案格式
- 示例命令的最终措辞

## Deferred Ideas

- `重构 main.py` — 先不作为 Phase 3 的锁定交付项
