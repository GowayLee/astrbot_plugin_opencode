---
phase: 03-直接聊天交互体验
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - main.py
  - core/executor.py
  - core/output.py
  - tests/core/test_executor_acp.py
  - tests/core/test_output_events.py
  - tests/test_main_commands.py
autonomous: true
requirements:
  - CHAT-01
  - CHAT-02
  - CHAT-03
must_haves:
  truths:
    - "用户发送 `/oc` 后，会先看到已接收/开始执行，而不是只等最终长文本"
    - "执行期间，AstrBot 能看到合并后的正文增量和极简工具流，而不是完全静默或逐小片段刷屏"
    - "最终结果仍走现有输出管线，不会把已流式展示的正文再原样重复一遍"
  artifacts:
    - path: "main.py"
      provides: "`/oc` 流式事件消费、chunk flush 与最终结果发送顺序"
      exports: ["OpenCodePlugin._run_oc_prompt", "OpenCodePlugin.oc_handler"]
    - path: "core/executor.py"
      provides: "runtime notification 继续归一化到 `message_chunk`/关键事件"
      exports:
        [
          "CommandExecutor._normalize_runtime_event",
          "CommandExecutor._stream_execution",
        ]
    - path: "core/output.py"
      provides: "聊天更新折叠、chunk 展示与最终结果去重策略"
      exports:
        [
          "OutputProcessor.build_chat_updates",
          "OutputProcessor.build_final_result_plan",
        ]
    - path: "tests/core/test_output_events.py"
      provides: "输出折叠与 chunk 展示回归覆盖"
    - path: "tests/test_main_commands.py"
      provides: "`/oc` 流式聊天路径回归覆盖"
  key_links:
    - from: "core/executor.py"
      to: "main.py"
      via: "`_stream_execution()` -> `_run_oc_prompt()` runtime event yield"
      pattern: "kind.*event|message_chunk|plan_updated|tool_started"
    - from: "main.py"
      to: "core/output.py"
      via: "`_run_oc_prompt()` 调 `build_chat_updates()` / `parse_output_plan()`"
      pattern: "build_chat_updates|parse_output_plan"
---

<objective>
先把 Phase 3 的基础聊天回传链路补完整：`/oc` 不再只有开始提示和最终答案，中间能够稳定回传正文增量与关键节点。

Purpose: 这一步直接落实 D-01、D-02、D-03、D-04、D-05；如果正文 chunk 和关键事件还不能稳定回到 AstrBot，后面的权限排版、帮助提示再好也只是包在“静默执行”外面的壳。
Output: 流式事件测试、chunk/关键节点折叠实现、以及不会重复刷屏的最终输出链路。
</objective>

<execution_context>
@/home/LiHaoyuan/workspace/cool_stuff/astrbot_plugin_opencode/.opencode/get-shit-done/workflows/execute-plan.md
@/home/LiHaoyuan/workspace/cool_stuff/astrbot_plugin_opencode/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-直接聊天交互体验/03-CONTEXT.md
@.planning/phases/02-会话内核与生命周期统一/02-04-SUMMARY.md
@.planning/phases/02.3-acp-opencode-invalid-params/02.3-02-SUMMARY.md
@main.py
@core/executor.py
@core/output.py
@tests/core/test_executor_acp.py
@tests/core/test_output_events.py
@tests/test_main_commands.py

<interfaces>
From `main.py`:
```python
async def _run_oc_prompt(self, event: AstrMessageEvent, session, prompt_payload: Any, emit_status: bool = True)
def _render_exec_status(self, session) -> str
```

From `core/output.py`:

```python
def build_chat_updates(self, events: list[ACPNormalizedEvent | dict[str, Any]], session: Optional[OpenCodeSession] = None) -> list[str]
async def build_final_result_plan(self, result: Any, event: AstrMessageEvent, session: Optional[OpenCodeSession] = None) -> List[List]
```

From `core/executor.py`:

```python
async def stream_prompt(self, prompt_payload: Any, session: OpenCodeSession)
def _normalize_runtime_event(self, method: str, params: Optional[dict[str, Any]] = None) -> dict[str, Any]
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先用测试钉死 `/oc` 的流式聊天契约</name>
  <files>tests/core/test_executor_acp.py, tests/core/test_output_events.py, tests/test_main_commands.py</files>
  <read_first>
    - tests/core/test_executor_acp.py
    - tests/core/test_output_events.py
    - tests/test_main_commands.py
    - core/executor.py
    - core/output.py
    - main.py
    - .planning/phases/03-直接聊天交互体验/03-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `session/update` 或直接 runtime notification 中的 `message_chunk` 会被保留下来，不会在 executor 或 output 层被静默吞掉（D-01, D-02）。
    - Test 2: 多个连续 `message_chunk` 进入聊天层后，会被合并成稳定的增量文本，而不是 1 chunk 1 条消息刷屏（D-03）。
    - Test 3: `run_started`、`plan_updated`、`tool_started/tool_updated`、`run_finished/run_failed` 仍然可见，且工具流只保留极简摘要（D-04, D-05）。
    - Test 4: 当正文已经通过流式路径发出后，最终 `parse_output_plan()` 不会再把同一段正文完整重复一次。
  </behavior>
  <action>在 `tests/core/test_executor_acp.py` 增加 `message_chunk` 的 runtime event 样例；在 `tests/core/test_output_events.py` 新增连续 chunk 合并、关键节点保留、最终结果去重断言；在 `tests/test_main_commands.py` 新增 `/oc` 流式场景，明确断言输出顺序至少为“执行开始 -> 合并后的正文增量/关键节点 -> 最终结果”，并且连续 chunk 不得拆成多条零碎消息。测试里的预期文本直接写死为中文聊天输出，不要只断言 `len(outputs) > 0` 这种宽松条件。</action>
  <acceptance_criteria>
    - `tests/core/test_executor_acp.py` 包含 `message_chunk` runtime notification 样例。
    - `tests/core/test_output_events.py` 包含“连续 chunk 合并成一条聊天更新”的断言。
    - `tests/test_main_commands.py` 包含 `/oc` 流式正文回传顺序断言，且明确检查 chunk 没有逐条刷屏。
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/core/test_executor_acp.py tests/core/test_output_events.py tests/test_main_commands.py -q</automated>
  </verify>
  <done>Phase 3 的聊天回传契约先被测试固定：正文 chunk、关键节点和最终结果之间的职责边界清晰可回归。</done>
</task>

<task type="auto">
  <name>Task 2: 落地合并后的正文增量与极简工具流展示</name>
  <files>main.py, core/executor.py, core/output.py</files>
  <read_first>
    - main.py
    - core/executor.py
    - core/output.py
    - tests/core/test_executor_acp.py
    - tests/core/test_output_events.py
    - tests/test_main_commands.py
    - .planning/phases/03-直接聊天交互体验/03-CONTEXT.md
  </read_first>
  <action>按 D-01~D-05 改造流式聊天链路：在 `core/executor.py` 保留并正常归一化 `message_chunk`；在 `main.py:_run_oc_prompt()` 中为连续 `message_chunk` 增加批量 flush 逻辑，采用“累计到 120 个字符、遇到换行、或距离上次 flush 超过 1.2 秒时输出一条合并消息；遇到 plan/tool/permission/finish 事件先 flush 再继续”的固定策略；在 `core/output.py` 中把 `message_chunk` 渲染成正文增量消息，把 `tool_started/tool_updated` 折叠成只带工具名与目标/命令摘要的一行提示，并确保 `build_final_result_plan()` 在最终 `final_text` 与已流式发出的正文一致时只补结束状态，不再重复整段正文。不要实现逐 token 推送，也不要把完整工具日志直接暴露到 IM。</action>
  <acceptance_criteria>
    - `main.py` 中存在针对 `message_chunk` 的缓冲/flush 分支，且阈值写成明确常量 `120` 或 `1.2` 秒级策略，而不是模糊“适当节流”。
    - `core/output.py` 不再对 `message_chunk` 直接返回空字符串，而是能产出正文增量文本。
    - `core/output.py` 的工具事件渲染仍是一行极简摘要，不输出完整工具详情列表。
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/core/test_executor_acp.py tests/core/test_output_events.py tests/test_main_commands.py -q</automated>
  </verify>
  <done>用户通过 `/oc` 执行任务时，AstrBot 能稳定展示“开始执行 + 合并后的正文增量 + 关键节点 + 最终结果”的完整聊天体验，而且不会被碎片化 chunk 或工具日志刷屏。</done>
</task>

</tasks>

<verification>
- `.venv/bin/python -m pytest tests/core/test_executor_acp.py tests/core/test_output_events.py tests/test_main_commands.py -q`
- 检查 `main.py` / `core/output.py` 中出现 `message_chunk`、flush 分支、最终结果去重逻辑
</verification>

<success_criteria>

- `/oc` 的中间态不再静默
- 正文增量采用合并后展示，而不是逐小片段刷屏
- 工具流只保留关键摘要，最终结果不重复整段已流式内容
  </success_criteria>

<output>
After completion, create `.planning/phases/03-直接聊天交互体验/03-01-SUMMARY.md`
</output>
