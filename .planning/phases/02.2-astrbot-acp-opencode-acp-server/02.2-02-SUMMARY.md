# Phase 02.2 / Plan 02 Summary

## Outcome

已补齐 OpenCode 参考与 `docs/references/` 统一入口：

- `docs/references/opencode/README.md`
- `docs/references/README.md`
- `docs/references/AGENTS.md`

## What Was Added

- OpenCode 参考文档：说明 `opencode acp` 的 `stdio` 工作方式、支持范围、`AGENTS.md` / `permissions` 等能力，以及 `/undo`、`/redo` 暂不支持的限制。
- 根索引：建立 `astrbot -> acp -> opencode` 的阅读顺序与文档地图。
- 子树约束：在 `docs/references/AGENTS.md` 明确官方原文优先、仓库整理稿不替代规范、优先维护单文件主文档。

## Verification

已通过关键字校验，确认根索引与子树说明满足计划中的标题、链接和维护规则要求。
