---
phase: 02.3-acp-opencode-invalid-params
verified: 2026-03-29T15:53:14Z
status: passed
score: 7/7 must-haves verified
re_verification:
  previous_status: gaps_found
  previous_score: 7/7
  gaps_closed:
    - "PHASE-02.3 的 requirement IDs 能在 REQUIREMENTS.md 中被逐条交叉验证"
  gaps_remaining: []
  regressions: []
---

# Phase 02.3: acp-opencode-invalid-params Verification Report

**Phase Goal:** 让 AstrBot 插件发给 `opencode acp` 的 initialize / session / prompt 主链路重新符合 ACP v1 协议，并恢复真实 OpenCode runtime update 的正确消费。
**Verified:** 2026-03-29T15:53:14Z
**Status:** passed
**Re-verification:** Yes — after gap closure

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                        | Status     | Evidence                                                                                                                                                                                        |
| --- | ------------------------------------------------------------------------------------------------------------ | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | ACP initialize 不再因为缺少 `protocolVersion` / `clientCapabilities` / `clientInfo.version` 被 OpenCode 拒绝 | ✓ VERIFIED | `core/acp_client.py:51-67` 发送 ACP v1 initialize；`core/executor.py:137-147` 传入完整 `client_info`；`tests/core/test_acp_client.py:162-194` 精确断言 request contract                         |
| 2   | 新建或恢复会话时，OpenCode 能收到带 `mcpServers` 的合法 session payload                                      | ✓ VERIFIED | `core/executor.py:465-470,508-516`；`tests/core/test_executor_acp.py:244-249,315-319`；`gsd-tools verify artifacts/key-links` 两份 plan 全通过                                                  |
| 3   | 首次 prompt 发送的是 ACP v1 `prompt` 数组，而不是旧字段 `contentBlocks`                                      | ✓ VERIFIED | `core/input.py:34-39` 输出 `prompt`；`core/executor.py:655-680` 统一归一化；静态检查结果 `contentBlocks=False`, `prompt=True`；`tests/core/test_executor_acp.py:656-674` 覆盖兼容输入           |
| 4   | initialize 成功后，executor 能读到 OpenCode 当前返回的 `agentCapabilities` 并继续决定是否 `loadSession`      | ✓ VERIFIED | `core/acp_client.py:61-65` 优先保存 `agentCapabilities`；`core/executor.py:457-465` 用 `protocol_capabilities.loadSession` 决策；`tests/core/test_executor_acp.py:252-289` 回归通过             |
| 5   | `session/update` 的 `params.update.sessionUpdate` 能被归一化成现有聊天事件，而不是静默丢失                   | ✓ VERIFIED | `core/executor.py:566-581,729-766` 先解包 `update` 再映射 `type`；`tests/core/test_executor_acp.py:677-824` 覆盖 `mode_updated` / `permission_requested`                                        |
| 6   | mode、config option、permission 这些运行态信息在 ACP v1 响应结构下仍能更新到 session 与输出链                | ✓ VERIFIED | `core/acp_adapter.py:21-115,138-177`；`core/executor.py:531-565,837-854`；`tests/core/test_acp_adapter.py:113-138,176-223`；`tests/core/test_output_events.py:152-178,266-293`                  |
| 7   | 真实 AstrBot 宿主中的 `/oc hello` 与 `/oc-session` 不再落回 `ACP 后端启动失败: opencode ... Invalid params`  | ✓ VERIFIED | `.planning/phases/02.3-acp-opencode-invalid-params/02.3-VALIDATION.md:56-61` 定义手工验收；本次验证采纳用户提供的真实宿主结果：`/oc hello` 与 `/oc-session` 均已通过且不再出现 `Invalid params` |

**Score:** 7/7 truths verified

### Required Artifacts

| Artifact                           | Expected                                                           | Status     | Details                                                                                               |
| ---------------------------------- | ------------------------------------------------------------------ | ---------- | ----------------------------------------------------------------------------------------------------- |
| `core/acp_client.py`               | ACP v1 initialize / capability 消费                                | ✓ VERIFIED | `gsd-tools verify artifacts`(plan 01) 通过；`initialize()` 发送标准字段并优先保存 `agentCapabilities` |
| `core/executor.py`                 | session/new、session/load、session/prompt 与 runtime update 主链路 | ✓ VERIFIED | `gsd-tools verify artifacts`(plan 01/02) 通过；payload、loadSession 判断、update 解包都存在           |
| `core/input.py`                    | `ACPPromptPayload` 转 ACP v1 `prompt`                              | ✓ VERIFIED | `to_payload()` 输出 `prompt`；静态检查不再把 `contentBlocks` 作为正式字段                             |
| `core/acp_adapter.py`              | ACP v1 mode/config/permission 归一化                               | ✓ VERIFIED | `normalize_session_state()` 支持 `modes.availableModes/currentModeId` 与 `agentCapabilities`          |
| `tests/core/test_acp_client.py`    | initialize 合同测试                                                | ✓ VERIFIED | request-level 精确断言存在                                                                            |
| `tests/core/test_executor_acp.py`  | session/new/load/prompt/runtime 合同测试                           | ✓ VERIFIED | 覆盖 `mcpServers`、`prompt`、`session/update`、`agentCapabilities`                                    |
| `tests/core/test_acp_adapter.py`   | ACP v1 mode/capability 归一化测试                                  | ✓ VERIFIED | 覆盖 `availableModes/currentModeId` 与 capability 读取                                                |
| `tests/core/test_output_events.py` | 输出链 runtime/permission 折叠测试                                 | ✓ VERIFIED | 覆盖权限提示与 runtime 事件折叠                                                                       |

### Key Link Verification

| From                 | To                                 | Via                                                     | Status  | Details                                    |
| -------------------- | ---------------------------------- | ------------------------------------------------------- | ------- | ------------------------------------------ |
| `core/executor.py`   | `core/acp_client.py`               | `client.initialize/new_session/prompt_session payload`  | ✓ WIRED | `gsd-tools verify key-links`(plan 01) 通过 |
| `core/input.py`      | `core/executor.py`                 | `ACPPromptPayload.to_payload -> _coerce_prompt_payload` | ✓ WIRED | `gsd-tools verify key-links`(plan 01) 通过 |
| `core/acp_client.py` | `core/executor.py`                 | `protocol_capabilities -> loadSession 判断`             | ✓ WIRED | `gsd-tools verify key-links`(plan 02) 通过 |
| `core/executor.py`   | `core/acp_adapter.py`              | `_apply_session_state / _normalize_runtime_event`       | ✓ WIRED | `gsd-tools verify key-links`(plan 02) 通过 |
| `core/executor.py`   | `tests/core/test_output_events.py` | `runtime permission + mode event folding`               | ✓ WIRED | `gsd-tools verify key-links`(plan 02) 通过 |

### Data-Flow Trace (Level 4)

| Artifact                                   | Data Variable                   | Source                                                                                                                     | Produces Real Data | Status    |
| ------------------------------------------ | ------------------------------- | -------------------------------------------------------------------------------------------------------------------------- | ------------------ | --------- |
| `core/input.py` + `core/executor.py`       | `prompt`                        | `ACPPromptPayload.content_blocks -> to_payload() -> _coerce_prompt_payload() -> client.prompt_session()`                   | Yes                | ✓ FLOWING |
| `core/acp_client.py` + `core/executor.py`  | `protocol_capabilities`         | initialize response `agentCapabilities` -> `client.protocol_capabilities` -> `_ensure_session_ready()`                     | Yes                | ✓ FLOWING |
| `core/executor.py` + `core/acp_adapter.py` | runtime `event` / session state | ACP notification `params.update` -> `_unwrap_session_payload()` -> `_normalize_runtime_event()` / `_apply_session_state()` | Yes                | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior                           | Command                                                                                                                                                       | Result                                                                            | Status |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- | ------ |
| Phase 02.3 ACP 回归测试全绿        | `.venv/bin/python -m pytest tests/core/test_acp_client.py tests/core/test_acp_adapter.py tests/core/test_executor_acp.py tests/core/test_output_events.py -q` | `40 passed in 2.10s`                                                              | ✓ PASS |
| request/runtime 关键链接可自动验证 | `gsd-tools verify artifacts (plan 01/02) + verify key-links (plan 01/02)`                                                                                     | plan 01 artifacts `5/5`、plan 02 artifacts `4/4`、key-links `2/2` 与 `3/3` 全通过 | ✓ PASS |
| 输入层正式字段已切换到 `prompt`    | `python -c "from pathlib import Path; ..."`                                                                                                                   | `contentBlocks=False`, `prompt=True`                                              | ✓ PASS |

### Requirements Coverage

| Requirement     | Source Plan       | Description                                                                                                                           | Status      | Evidence                                                                                                                                                                                                |
| --------------- | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `PHASE-02.3-01` | `02.3-01-PLAN.md` | 插件发给 `opencode acp` 的 `initialize` 请求必须符合 ACP v1，包含 `protocolVersion`、`clientCapabilities` 与完整 `clientInfo.version` | ✓ SATISFIED | `.planning/REQUIREMENTS.md:38`；`core/acp_client.py:56-60`；`tests/core/test_acp_client.py:169-190`                                                                                                     |
| `PHASE-02.3-02` | `02.3-01-PLAN.md` | 插件发给 `session/new` 与 `session/load` 的 payload 必须显式携带 `mcpServers: []`                                                     | ✓ SATISFIED | `.planning/REQUIREMENTS.md:39`；`core/executor.py:465-470,509-516`；`tests/core/test_executor_acp.py:244-249,315-319`                                                                                   |
| `PHASE-02.3-03` | `02.3-01-PLAN.md` | 插件发给 `session/prompt` 的正式字段必须是 ACP v1 `prompt` 数组，而不是旧字段 `contentBlocks`                                         | ✓ SATISFIED | `.planning/REQUIREMENTS.md:40`；`core/input.py:34-39`；`core/executor.py:655-680`；`tests/core/test_executor_acp.py:656-674`                                                                            |
| `PHASE-02.3-04` | `02.3-02-PLAN.md` | initialize 返回的 `agentCapabilities`、session state 的 mode/config 结构，以及 `session/update` 通知都必须被当前执行链正确消费        | ✓ SATISFIED | `.planning/REQUIREMENTS.md:41`；`core/acp_client.py:61-65`；`core/acp_adapter.py:21-115`；`core/executor.py:531-565,729-766,837-854`；相关 pytest 全绿                                                  |
| `PHASE-02.3-05` | `02.3-02-PLAN.md` | 在真实 AstrBot 宿主中执行 `/oc hello` 与 `/oc-session` 时，不再出现 `ACP 后端启动失败: opencode` 包裹的 `Invalid params`              | ✓ SATISFIED | `.planning/REQUIREMENTS.md:42`；`.planning/phases/02.3-acp-opencode-invalid-params/02.3-VALIDATION.md:58-61`；用户提供的真实宿主验证结果显示 `/oc hello` 与 `/oc-session` 均已通过且无 `Invalid params` |

**Requirement/accounting result:** 两份 PLAN frontmatter 声明的 `PHASE-02.3-01 ~ PHASE-02.3-05` 均已在 `.planning/REQUIREMENTS.md` 中定义并逐条交叉验证，无 orphaned requirement。

### Anti-Patterns Found

| File               | Line              | Pattern                   | Severity | Impact                                                                                                           |
| ------------------ | ----------------- | ------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------- |
| `core/executor.py` | 99, 607, 734, 835 | `return {}` / `return []` | ℹ️ Info  | 均为防御性空返回；本 phase 的真实数据流由上方 payload 解析、session update 解包与测试覆盖证明已接通，不构成 stub |

### Human Verification Required

无。上一版报告里的两项人工冒烟已由用户在真实 AstrBot 宿主完成，并与当前代码实现一致。

### Gaps Summary

上一版唯一缺口是需求追踪：PLAN 中声明的 `PHASE-02.3-01 ~ PHASE-02.3-05` 当时无法在 `.planning/REQUIREMENTS.md` 中逐条对照。当前该缺口已关闭，五个 requirement IDs 均已在需求清单中定义并可映射到代码或真实宿主证据。

代码层面未发现回归：ACP v1 initialize/session/prompt 请求、`agentCapabilities` 消费、`session/update` 解包、mode/config/permission 状态刷新、输出链折叠和相关回归测试全部成立；再结合用户提供的真实宿主 `/oc hello` 与 `/oc-session` 冒烟结果，Phase 02.3 目标已达成。

---

_Verified: 2026-03-29T15:53:14Z_  
_Verifier: the agent (gsd-verifier)_
