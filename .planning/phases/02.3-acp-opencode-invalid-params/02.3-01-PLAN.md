---
phase: 02.3-acp-opencode-invalid-params
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/acp_client.py
  - core/executor.py
  - core/input.py
  - tests/core/test_acp_client.py
  - tests/core/test_executor_acp.py
autonomous: true
requirements:
  - PHASE-02.3-01
  - PHASE-02.3-02
  - PHASE-02.3-03
must_haves:
  truths:
    - "ACP initialize 不再因为缺少 protocolVersion/clientInfo.version/clientCapabilities 而被 OpenCode 拒绝"
    - "新建或恢复会话时，OpenCode 能收到带 mcpServers 的合法 session payload"
    - "首次 prompt 发送的是 ACP v1 `prompt` 数组，而不是仓库旧字段 `contentBlocks`"
  artifacts:
    - path: "core/acp_client.py"
      provides: "ACP v1 initialize 请求组装"
      exports:
        [
          "ACPClient.initialize",
          "ACPClient.new_session",
          "ACPClient.prompt_session",
        ]
    - path: "core/executor.py"
      provides: "session/new、session/load、session/prompt 的合法 payload"
      exports:
        [
          "CommandExecutor.initialize_if_needed",
          "CommandExecutor._create_session",
          "CommandExecutor._coerce_prompt_payload",
        ]
    - path: "core/input.py"
      provides: "ACPPromptPayload 转 ACP v1 prompt 结构"
      exports: ["ACPPromptPayload.to_payload"]
    - path: "tests/core/test_acp_client.py"
      provides: "initialize request contract 覆盖"
    - path: "tests/core/test_executor_acp.py"
      provides: "session/new 与 session/prompt request contract 覆盖"
  key_links:
    - from: "core/executor.py"
      to: "core/acp_client.py"
      via: "client.initialize/new_session/prompt_session payload"
      pattern: "protocolVersion|clientCapabilities|mcpServers|prompt"
    - from: "core/input.py"
      to: "core/executor.py"
      via: "ACPPromptPayload.to_payload -> _coerce_prompt_payload"
      pattern: "to_payload|prompt"
---

<objective>
把 initialize、session/new、session/load、session/prompt 的请求形状一次性对齐到 ACP v1，让当前 `Invalid params` 主链路先停下来。

Purpose: 这是本 phase 的止血点；只有请求字段先合法，后续 response/update 解析才有机会被真实执行到。
Output: 协议契约测试 + `core/acp_client.py` / `core/executor.py` / `core/input.py` 的 ACP v1 request shape 修复。
</objective>

<execution_context>
@/home/LiHaoyuan/workspace/cool_stuff/astrbot_plugin_opencode/.opencode/get-shit-done/workflows/execute-plan.md
@/home/LiHaoyuan/workspace/cool_stuff/astrbot_plugin_opencode/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.3-acp-opencode-invalid-params/02.3-RESEARCH.md
@.planning/phases/02.3-acp-opencode-invalid-params/02.3-VALIDATION.md
@core/acp_client.py
@core/executor.py
@core/input.py
@tests/core/test_acp_client.py
@tests/core/test_executor_acp.py

<interfaces>
From `core/acp_client.py`:
```python
async def initialize(
    self,
    client_capabilities: Optional[dict[str, Any]] = None,
    client_info: Optional[dict[str, Any]] = None,
) -> dict[str, Any]

async def new_session(self, payload: Optional[dict[str, Any]] = None) -> dict[str, Any]
async def load_session(self, payload: Optional[dict[str, Any]] = None) -> dict[str, Any]
async def prompt_session(self, payload: Optional[dict[str, Any]] = None) -> dict[str, Any]

````

From `core/input.py`:
```python
class ACPPromptPayload(str):
    def to_payload(self) -> dict:
        return {
            "text": self.text,
            "contentBlocks": copy.deepcopy(self.content_blocks),
            **copy.deepcopy(self.metadata),
        }
````

From `core/executor.py`:

```python
def _coerce_prompt_payload(self, prompt_input: Any) -> dict[str, Any]
async def _create_session(self, session: OpenCodeSession) -> ExecutionResult
async def _ensure_session_ready(self, session: OpenCodeSession, allow_recreate_after_load_failure: bool = True) -> SessionEnsureResult
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先把 ACP v1 request contract 固定进测试</name>
  <files>tests/core/test_acp_client.py, tests/core/test_executor_acp.py</files>
  <read_first>
    - tests/core/test_acp_client.py
    - tests/core/test_executor_acp.py
    - core/acp_client.py
    - core/executor.py
    - core/input.py
    - .planning/phases/02.3-acp-opencode-invalid-params/02.3-RESEARCH.md
    - .planning/phases/02.3-acp-opencode-invalid-params/02.3-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: `ACPClient.initialize()` 发送的 request params 必须包含 `protocolVersion: 1`、`clientCapabilities`、`clientInfo.name`、`clientInfo.title`、`clientInfo.version`，且不再发送顶层 `capabilities`。
    - Test 2: `CommandExecutor._create_session()` 发出的 `session/new` payload 必须包含 `cwd` 与 `mcpServers: []`，并保留现有 `agent`、`mode`、`config` 默认值透传。
    - Test 3: `CommandExecutor._ensure_session_ready()` 的 `session/load` payload 也必须包含 `sessionId`、`cwd`、`mcpServers: []`。
    - Test 4: `ACPPromptPayload.to_payload()` 与 `_coerce_prompt_payload()` 产出的结构必须是 `prompt: ContentBlock[]`，而不是 `contentBlocks`。
  </behavior>
  <action>在 `tests/core/test_acp_client.py` 新增 transport 记录型断言，直接检查 `initialize` 发送报文中的字段名和值；在 `tests/core/test_executor_acp.py` 把现有所有基于 `contentBlocks` 的断言改为 `prompt`，并新增 `mcpServers: []`、`clientInfo.title == "ACP Client"`、`clientInfo.version == "1.3.1"` 这些明确值的断言。不要写“字段存在即可”的宽松测试，必须精确到键名与关键常量，确保这轮不是热修一半。</action>
  <acceptance_criteria>
    - `tests/core/test_acp_client.py` 包含 `protocolVersion`、`clientCapabilities`、`clientInfo`、`version` 的 request-level 断言。
    - `tests/core/test_executor_acp.py` 不再把 `contentBlocks` 作为成功路径断言键名，改为断言 `prompt`。
    - `tests/core/test_executor_acp.py` 对 `session/new` 与 `session/load` 都断言 `mcpServers == []`。
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/core/test_acp_client.py tests/core/test_executor_acp.py -q</automated>
  </verify>
  <done>测试先把 initialize / session/new / session/load / session/prompt 的 ACP v1 契约钉死，执行器实现若仍发旧字段会直接红灯。</done>
</task>

<task type="auto">
  <name>Task 2: 按测试把 request payload 全部切到 ACP v1</name>
  <files>core/acp_client.py, core/executor.py, core/input.py</files>
  <read_first>
    - core/acp_client.py
    - core/executor.py
    - core/input.py
    - tests/core/test_acp_client.py
    - tests/core/test_executor_acp.py
    - .planning/phases/02.3-acp-opencode-invalid-params/02.3-RESEARCH.md
  </read_first>
  <action>在 `core/acp_client.py` 中把 `initialize()` 改成发送 `{"protocolVersion": 1, "clientCapabilities": <config>, "clientInfo": {"name": "astrbot_plugin_acp", "title": "ACP Client", "version": "1.3.1"}}`，并把 `protocol_capabilities` 改为优先记录 `response.agentCapabilities`；在 `core/executor.py` 中让 `initialize_if_needed()` 传入完整 `client_info`，让 `_create_session()` 与 `_ensure_session_ready()` 都显式附带 `mcpServers: []`，同时 `_coerce_prompt_payload()` 对 dict / `ACPPromptPayload` / 纯文本三种输入统一产出 `prompt` 数组；在 `core/input.py` 中把 `ACPPromptPayload.to_payload()` 的正式字段从 `contentBlocks` 改为 `prompt`，保留 `text` 仅作本地兼容辅助。不要把协议字段散回 `main.py`，也不要新增“旧字段兜底优先”的双写逻辑。</action>
  <acceptance_criteria>
    - `core/acp_client.py` 中 `initialize` request params 明确包含 `protocolVersion`、`clientCapabilities`、`clientInfo`，且不再写入 `"capabilities":`。
    - `core/executor.py` 中 `session/new` 与 `session/load` payload 明确包含 `"mcpServers": []`。
    - `core/input.py` 与 `core/executor.py` 的 prompt 构造路径使用 `prompt` 作为 ACP 正式字段。
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/core/test_acp_client.py tests/core/test_executor_acp.py -q</automated>
  </verify>
  <done>本地协议客户端发出的第一批请求已经符合 ACP v1；真实 OpenCode 不会再在 initialize / session/new / session/prompt 这三跳上因为旧字段直接回 `Invalid params`。</done>
</task>

</tasks>

<verification>
- `.venv/bin/python -m pytest tests/core/test_acp_client.py tests/core/test_executor_acp.py -q`
- grep 关键字段：`protocolVersion|clientCapabilities|mcpServers|prompt`
</verification>

<success_criteria>

- initialize request 使用 ACP v1 标准字段并带完整 clientInfo
- session/new 与 session/load 都带 `mcpServers: []`
- prompt 主链路彻底从 `contentBlocks` 切换到 `prompt`
  </success_criteria>

<output>
After completion, create `.planning/phases/02.3-acp-opencode-invalid-params/02.3-01-SUMMARY.md`
</output>
