---
phase: 02.3-acp-opencode-invalid-params
plan: 02
subsystem: api
tags: [acp, opencode, session-update, modes, permissions]

# Dependency graph
requires:
  - phase: 02.3-acp-opencode-invalid-params
    provides: ACP v1 initialize/session/prompt request shape
provides:
  - ACP v1 runtime event 解包与 session/update 归一化
  - modes.availableModes/currentModeId 到现有 session 状态的映射
  - agentCapabilities、权限请求与运行态消息的回归测试
affects: [phase-03, runtime-events, session-recovery, permission-flow]

# Tech tracking
tech-stack:
  added: []
  patterns:
    [
      session/update unwrap before output pipeline,
      ACP v1 mode-object normalization,
    ]

key-files:
  created: []
  modified:
    - core/acp_adapter.py
    - core/executor.py
    - tests/core/test_acp_adapter.py
    - tests/core/test_executor_acp.py

key-decisions:
  - "先在 executor 里统一解包 params.update，再把 sessionUpdate 映射回现有 event.type，保持 output pipeline 不变。"
  - "把 ACP v1 的 modes 对象与 agentCapabilities 在 adapter 层归一化，避免状态消费分散到上层调用点。"

patterns-established:
  - "Pattern 1: runtime 通知先扁平化 session/update，再走现有 permission/mode 事件折叠链。"
  - "Pattern 2: session state 兼容 modes.availableModes/currentModeId 与 agentCapabilities 双入口。"

requirements-completed: [PHASE-02.3-04, PHASE-02.3-05]

# Metrics
duration: 2 min
completed: 2026-03-29
---

# Phase 02.3 Plan 02: 修正 response/update 归一化并补上真实宿主冒烟标准 Summary

**ACP v1 的 session/update、mode 对象和权限请求现在都能落回现有会话状态与输出链，不再因旧 schema 假设静默丢失。**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-29T15:01:08Z
- **Completed:** 2026-03-29T15:03:33Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- 新增 ACP v1 回归测试，固定 `agentCapabilities`、`modes.availableModes/currentModeId` 与 `session/update -> params.update.sessionUpdate` 的真实形状。
- 让 executor 在消费 runtime 通知前先解包 `params.update`，并把 `sessionUpdate` 回填到现有 `event["type"]`，继续复用当前 output pipeline。
- 让 adapter/session 状态兼容 ACP v1 的 mode 对象与 `agentCapabilities`，并在执行摘要里补上真实宿主冒烟标准：`/oc hello` 与 `/oc-session` 都不应再回落到 `Invalid params`。

## Task Commits

Each task was committed atomically:

1. **Task 1: 先补 ACP v1 response 与 runtime update 归一化测试** - `f5da3ed` (test)
2. **Task 2: 修正 response/update 消费，并写入真实宿主冒烟标准** - `ee494f3` (feat)

**Plan metadata:** `06e722c` (docs)

## Files Created/Modified

- `tests/core/test_acp_adapter.py` - 固定 ACP v1 mode 对象与 `agentCapabilities` 归一化契约。
- `tests/core/test_executor_acp.py` - 固定 `session/update` 解包、权限请求与运行态 session 刷新行为。
- `core/acp_adapter.py` - 兼容 `modes.availableModes/currentModeId` 与 `agentCapabilities`。
- `core/executor.py` - 统一解包 `params.update`，并把 `sessionUpdate` 映射到现有 runtime 事件类型。

## Decisions Made

- 保持 output 层不感知 ACP v1 包裹结构，统一由 executor 做 `session/update` 解包与事件类型归一化。
- mode/config/capability 的 ACP v1 兼容统一收口到 adapter/session state 应用层，不把协议细节散到命令编排层。

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] `roadmap update-plan-progress` 未把 02.3 行写成完成态**

- **Found during:** 计划收尾元数据回写
- **Issue:** gsd-tools 返回已更新，但 `ROADMAP.md` 的 02.3 行仍停留在 `1/2 In Progress`。
- **Fix:** 手动把 02.3 进度行修正为 `2/2 Complete (2026-03-29)`，保持磁盘状态与本计划结果一致。
- **Files modified:** `.planning/ROADMAP.md`
- **Verification:** 复读 `ROADMAP.md`，确认 02.3 行显示 `2/2 | Complete | 2026-03-29`。
- **Committed in:** pending metadata commit

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** 仅影响计划元数据回写，不影响 ACP v1 runtime 修复本身。

## Issues Encountered

- `requirements mark-complete PHASE-02.3-04 PHASE-02.3-05` 返回 `not_found`，因为当前 `.planning/REQUIREMENTS.md` 只维护产品级 CONF/CHAT/TOOL/SESS/SAFE 条目，没有 phase 级 requirement ID；本次保留 SUMMARY frontmatter 中的 requirement 记录，不扩写全局需求清单。

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 02.3 的请求与响应两侧 ACP v1 兼容已补齐，接下来只需按本摘要里的真实宿主冒烟标准完成 `/oc hello` 与 `/oc-session` 验证闭环。
- 当前无已知代码级阻塞；若真实宿主仍失败，应优先排查认证或宿主加载环境，而不是继续回退到旧协议字段。

---

_Phase: 02.3-acp-opencode-invalid-params_
_Completed: 2026-03-29_

## Self-Check: PASSED

- FOUND: `.planning/phases/02.3-acp-opencode-invalid-params/02.3-02-SUMMARY.md`
- FOUND: `f5da3ed`
- FOUND: `ee494f3`
- FOUND: `06e722c`
