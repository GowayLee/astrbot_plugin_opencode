---
phase: 02.3-acp-opencode-invalid-params
plan: 02
type: execute
wave: 2
depends_on:
  - 02.3-01
files_modified:
  - core/acp_client.py
  - core/acp_adapter.py
  - core/executor.py
  - tests/core/test_acp_adapter.py
  - tests/core/test_executor_acp.py
  - tests/core/test_output_events.py
autonomous: true
requirements:
  - PHASE-02.3-04
  - PHASE-02.3-05
must_haves:
  truths:
    - "initialize 成功后，executor 能读到 OpenCode 当前返回的 agentCapabilities 并继续决定是否 loadSession"
    - "session/update 的 `params.update.sessionUpdate` 能被归一化成现有聊天事件，而不是静默丢失"
    - "mode、config option、permission 这些运行态信息在 ACP v1 响应结构下仍能更新到 session"
    - "执行阶段有明确的真实宿主冒烟步骤，能验证 `/oc hello` 与 `/oc-session` 不再落回 Invalid params"
  artifacts:
    - path: "core/acp_adapter.py"
      provides: "ACP v1 session state / permission payload 归一化"
      exports:
        [
          "OpenCodeACPAdapter.extract_mode_view",
          "OpenCodeACPAdapter.normalize_session_state",
          "OpenCodeACPAdapter.normalize_permission_request",
        ]
    - path: "core/executor.py"
      provides: "runtime event 解包与 session 状态应用"
      exports:
        [
          "CommandExecutor._apply_session_state",
          "CommandExecutor._normalize_runtime_event",
        ]
    - path: "tests/core/test_acp_adapter.py"
      provides: "新 response shape 的归一化覆盖"
    - path: "tests/core/test_output_events.py"
      provides: "session/update 事件折叠与权限提示覆盖"
  key_links:
    - from: "core/acp_client.py"
      to: "core/executor.py"
      via: "protocol_capabilities -> loadSession 判断"
      pattern: "agentCapabilities|loadSession"
    - from: "core/executor.py"
      to: "core/acp_adapter.py"
      via: "_apply_session_state / _normalize_runtime_event"
      pattern: "sessionUpdate|availableModes|currentModeId"
    - from: "core/executor.py"
      to: "tests/core/test_output_events.py"
      via: "runtime permission + mode event folding"
      pattern: "permission_requested|mode_updated"
---

<objective>
在请求能通过之后，把 response / runtime update 的消费逻辑也切到 ACP v1，避免下一跳又因为旧 schema 假设而丢能力、丢事件、丢状态。

Purpose: 只修 request 不够；如果 `agentCapabilities`、`modes.availableModes/currentModeId`、`params.update.sessionUpdate` 还按旧结构读，Phase 2 已做好的会话恢复、mode 显示、权限过程提示都会继续失真。
Output: response/update 归一化修复、回归测试、以及执行阶段要跑的真实宿主冒烟标准。
</objective>

<execution_context>
@/home/LiHaoyuan/workspace/cool_stuff/astrbot_plugin_opencode/.opencode/get-shit-done/workflows/execute-plan.md
@/home/LiHaoyuan/workspace/cool_stuff/astrbot_plugin_opencode/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.3-acp-opencode-invalid-params/02.3-RESEARCH.md
@.planning/phases/02.3-acp-opencode-invalid-params/02.3-VALIDATION.md
@.planning/phases/02-会话内核与生命周期统一/02-01-SUMMARY.md
@.planning/phases/02-会话内核与生命周期统一/02-02-SUMMARY.md
@core/acp_client.py
@core/acp_adapter.py
@core/executor.py
@tests/core/test_acp_adapter.py
@tests/core/test_executor_acp.py
@tests/core/test_output_events.py

<interfaces>
From `core/acp_client.py`:
```python
self.protocol_capabilities: dict[str, Any] = {}
self.protocol_info: dict[str, Any] = {}
```

From `core/acp_adapter.py`:

```python
def extract_mode_view(
    self,
    config_options: Optional[list[dict[str, Any]]] = None,
    modes: Optional[list[dict[str, Any]]] = None,
    current_config_values: Optional[dict[str, Any]] = None,
    current_mode_id: Optional[str] = None,
) -> ACPModeView

def normalize_session_state(self, session_payload: Optional[dict[str, Any]] = None) -> ACPSessionState
def normalize_permission_request(self, payload: Optional[dict[str, Any]] = None) -> ACPPermissionRequest
```

From `core/executor.py`:

```python
def _apply_session_state(self, session: OpenCodeSession, payload: Optional[dict[str, Any]]) -> None
def _normalize_runtime_event(self, method: str, params: Optional[dict[str, Any]] = None) -> dict[str, Any]
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先补 ACP v1 response 与 runtime update 归一化测试</name>
  <files>tests/core/test_acp_adapter.py, tests/core/test_executor_acp.py, tests/core/test_output_events.py</files>
  <read_first>
    - tests/core/test_acp_adapter.py
    - tests/core/test_executor_acp.py
    - tests/core/test_output_events.py
    - core/acp_adapter.py
    - core/executor.py
    - core/acp_client.py
    - .planning/phases/02.3-acp-opencode-invalid-params/02.3-RESEARCH.md
    - .planning/phases/02-会话内核与生命周期统一/02-01-SUMMARY.md
    - .planning/phases/02-会话内核与生命周期统一/02-02-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: initialize response 中的 `agentCapabilities.loadSession` 能驱动 executor 的恢复分支，不能继续只读 `capabilities.loadSession`。
    - Test 2: session state 中 `modes: {availableModes, currentModeId}` 能正确更新 `session.current_mode_id` 与 `session.available_modes`。
    - Test 3: `session/update` 的 `params.update.sessionUpdate` 事件能被转成当前输出链可消费的 `type`，例如 `mode_updated`、`permission_requested`。
    - Test 4: 权限请求既支持 `session/request_permission`，也支持 `session/update` 包裹的 ACP v1 形状，不丢 `requestId/sessionId/options`。
  </behavior>
  <action>更新三份测试，让它们明确使用 ACP v1 response shape：initialize 返回 `agentCapabilities`；session payload 的 mode 来源改为 `modes.availableModes` + `modes.currentModeId`；runtime 通知改为 `{"method": "session/update", "params": {"sessionId": "...", "update": {"sessionUpdate": "mode_updated", ...}}}`。不要保留“旧结构也算通过”的模糊断言，测试要明确证明 executor/adapter 已真的理解新结构。</action>
  <acceptance_criteria>
    - `tests/core/test_executor_acp.py` 包含 `agentCapabilities` 相关断言。
    - `tests/core/test_acp_adapter.py` 包含 `availableModes` 与 `currentModeId` 相关断言。
    - `tests/core/test_output_events.py` 或 `tests/core/test_executor_acp.py` 包含 `params.update.sessionUpdate` 相关通知样例。
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/core/test_acp_adapter.py tests/core/test_executor_acp.py tests/core/test_output_events.py -q</automated>
  </verify>
  <done>ACP v1 响应/通知形状先被测试固定，后续实现如果继续按旧 payload 读会立刻失败。</done>
</task>

<task type="auto">
  <name>Task 2: 修正 response/update 消费，并写入真实宿主冒烟标准</name>
  <files>core/acp_client.py, core/acp_adapter.py, core/executor.py</files>
  <read_first>
    - core/acp_client.py
    - core/acp_adapter.py
    - core/executor.py
    - tests/core/test_acp_adapter.py
    - tests/core/test_executor_acp.py
    - tests/core/test_output_events.py
    - .planning/phases/02.3-acp-opencode-invalid-params/02.3-RESEARCH.md
    - .planning/phases/02.3-acp-opencode-invalid-params/02.3-VALIDATION.md
  </read_first>
  <action>在 `core/acp_client.py` 里把 initialize 返回能力的首选读取改为 `agentCapabilities`，`protocol_info` 保留完整响应；在 `core/acp_adapter.py` 里让 `normalize_session_state()` 兼容 `modes` 为对象时读取 `availableModes/currentModeId`，并让 capability 来源优先读取 `capabilities` 与 `agentCapabilities` 中真实存在的那个；在 `core/executor.py` 里把 `_normalize_runtime_event()` 改为先解包 `params.update`，再把 `update.sessionUpdate` 映射到现有 `type`，同时 `_apply_session_state()` 要能处理 `modes` 为对象、`availableCommands` / `configOptions` / `currentConfigValues` 嵌在 update 内的情况。保持事件最终仍走现有 output pipeline，不要把格式化逻辑塞进 executor。最后，在计划执行摘要中要求记录一次真实宿主冒烟：`/oc hello` 与 `/oc-session` 都不能再出现 `Invalid params`。”</action>
  <acceptance_criteria>
    - `core/acp_client.py` 中 `protocol_capabilities` 优先来自 `agentCapabilities`。
    - `core/acp_adapter.py` 能从 `modes.availableModes/currentModeId` 解析当前 mode 与可用 mode 列表。
    - `core/executor.py` 对 `session/update` 先解包 `params.update`，并把 `sessionUpdate` 写回 `event["type"]`。
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/core/test_acp_adapter.py tests/core/test_executor_acp.py tests/core/test_output_events.py -q</automated>
  </verify>
  <done>Phase 2 已经建立好的会话恢复、mode 展示、权限确认和过程消息，在 ACP v1 / OpenCode 1.3.5 的真实 response/update 结构下继续成立；执行人也有明确的真实宿主冒烟标准可照着验。</done>
</task>

</tasks>

<verification>
- `.venv/bin/python -m pytest tests/core/test_acp_adapter.py tests/core/test_executor_acp.py tests/core/test_output_events.py -q`
- 执行阶段额外手工冒烟：真实宿主中 `/oc hello`、`/oc-session`
</verification>

<success_criteria>

- executor 能用 `agentCapabilities.loadSession` 做恢复判断
- mode/config/permission/runtime update 在 ACP v1 结构下仍能更新到 session 和输出链
- 执行阶段拥有明确的真实宿主冒烟标准来关闭 PHASE-02.3-05
  </success_criteria>

<output>
After completion, create `.planning/phases/02.3-acp-opencode-invalid-params/02.3-02-SUMMARY.md`
</output>
