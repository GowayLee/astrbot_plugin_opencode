---
phase: 02.3-acp-opencode-invalid-params
plan: 01
subsystem: api
tags: [acp, opencode, json-rpc, pytest]

# Dependency graph
requires:
  - phase: 2
    provides: 统一的 ACP 会话执行内核与生命周期入口
provides:
  - ACP v1 initialize 请求字段
  - 带 mcpServers 的 session/new 与 session/load payload
  - 统一使用 prompt 数组的 prompt payload 归一化
affects: [02.3-02, core/acp_client.py, core/executor.py, core/input.py]

# Tech tracking
tech-stack:
  added: []
  patterns: [ACP v1 request contract pinning, prompt payload normalization]

key-files:
  created:
    [.planning/phases/02.3-acp-opencode-invalid-params/02.3-01-SUMMARY.md]
  modified:
    [
      core/acp_client.py,
      core/executor.py,
      core/input.py,
      tests/core/test_acp_client.py,
      tests/core/test_executor_acp.py,
    ]

key-decisions:
  - "initialize 固定发送 protocolVersion/clientCapabilities/clientInfo，避免继续沿用旧 capabilities 字段。"
  - "prompt 输入在 executor 层统一归一化为 prompt 数组，同时保留 text 仅作本地兼容辅助。"

patterns-established:
  - "ACP request shape 先由测试钉死，再按协议常量回填实现。"
  - "session/new 与 session/load 即使没有 MCP 也显式发送 mcpServers: []。"

requirements-completed: [PHASE-02.3-01, PHASE-02.3-02, PHASE-02.3-03]

# Metrics
duration: 1 min
completed: 2026-03-29
---

# Phase 02.3 Plan 01: 对齐 initialize / session / prompt 的 ACP v1 request shape Summary

**ACP v1 初始化、会话创建/恢复与 prompt 请求全部改成 OpenCode 1.3.5 可接受的标准字段。**

## Performance

- **Duration:** 1 min
- **Started:** 2026-03-29T14:53:55Z
- **Completed:** 2026-03-29T14:55:40Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- 用失败测试把 initialize、session/new、session/load、session/prompt 的 ACP v1 契约钉死。
- `ACPClient.initialize()` 改为发送 `protocolVersion`、`clientCapabilities` 和完整 `clientInfo`。
- `CommandExecutor` 与 `ACPPromptPayload` 统一改为发送 `mcpServers: []` 和 `prompt` 数组。

## Task Commits

Each task was committed atomically:

1. **Task 1: 先把 ACP v1 request contract 固定进测试** - `6b8cca8` (test)
2. **Task 2: 按测试把 request payload 全部切到 ACP v1** - `b0c2fd1` (feat)

**Plan metadata:** pending

## Files Created/Modified

- `tests/core/test_acp_client.py` - 新增 initialize request-level 契约断言。
- `tests/core/test_executor_acp.py` - 覆盖 session/new、session/load、session/prompt 的 ACP v1 请求形状。
- `core/acp_client.py` - 按 ACP v1 组装 initialize payload，并优先记录 `agentCapabilities`。
- `core/executor.py` - 会话与 prompt payload 全面切到 `mcpServers` / `prompt` 结构。
- `core/input.py` - `ACPPromptPayload.to_payload()` 改为输出 `prompt`。

## Decisions Made

- initialize 客户端身份固定为 `astrbot_plugin_acp / ACP Client / 1.3.1`，与本 phase 协议契约保持一致。
- prompt 兼容收口放在 `core/executor.py`，避免把 ACP 字段名散到调用层。

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] 手动修正 ROADMAP 的 Phase 2.3 计划进度行**

- **Found during:** Plan wrap-up
- **Issue:** `roadmap update-plan-progress "02.3"` 返回成功，但 `ROADMAP.md` 里的 2.3 行仍停在 `0/0`。
- **Fix:** 保留 gsd-tools 调用，再手动把 Phase 2.3 行改为 `1/2 / In Progress`。
- **Files modified:** `.planning/ROADMAP.md`, `.planning/phases/02.3-acp-opencode-invalid-params/02.3-01-SUMMARY.md`
- **Verification:** 重新读取 `ROADMAP.md`，确认 2.3 行显示 `1/2`。
- **Committed in:** pending (plan metadata commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** 仅影响计划元数据同步，不影响 ACP v1 request 修复结果。

## Issues Encountered

- `requirements mark-complete PHASE-02.3-01 PHASE-02.3-02 PHASE-02.3-03` 返回 `not_found`，说明这些 phase 级 ID 还未进入 `REQUIREMENTS.md` 的追踪表；本次未改动需求清单结构。

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- request 主链路已经对齐 ACP v1，可继续执行 `02.3-02` 处理 response/update 归一化。
- 当前自动化验证为 `.venv/bin/python -m pytest tests/core/test_acp_client.py tests/core/test_executor_acp.py -q`，结果已通过。

## Self-Check: PASSED

- FOUND: `.planning/phases/02.3-acp-opencode-invalid-params/02.3-01-SUMMARY.md`
- FOUND: `6b8cca8`
- FOUND: `b0c2fd1`
