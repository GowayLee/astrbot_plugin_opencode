---
phase: 02-会话内核与生命周期统一
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - core/session.py
  - core/executor.py
  - tests/core/test_executor_acp.py
  - tests/core/test_session_state.py
autonomous: true
requirements:
  - SESS-01
gap_closure: true
must_haves:
  truths:
    - "同一 sender 连续执行 `/oc` 时，会继续使用已建立的 live backend session，而不是再建一个新 session"
    - "sender 绑定的是历史 session ID 时，执行器能区分这是待恢复绑定而不是当前连接里的 live 会话"
    - "历史 session 恢复失败后，`/oc` 会先清掉坏绑定，再回到可继续执行的新会话状态"
  artifacts:
    - path: "core/session.py"
      provides: "区分 live 会话续用与历史会话恢复的 sender 状态模型"
      exports:
        [
          "OpenCodeSession.bind_backend_session",
          "OpenCodeSession.drop_backend_session",
          "OpenCodeSession.reset_live_session",
        ]
    - path: "core/executor.py"
      provides: "按 live/history 分流的 ACP 会话 ensure 逻辑"
      exports:
        [
          "CommandExecutor.ensure_session",
          "CommandExecutor.load_session",
          "CommandExecutor._ensure_session_ready",
        ]
    - path: "tests/core/test_executor_acp.py"
      provides: "连续 `/oc` 续会话与坏历史绑定恢复的回归覆盖"
    - path: "tests/core/test_session_state.py"
      provides: "sender 会话 live/history 状态边界覆盖"
  key_links:
    - from: "core/session.py"
      to: "core/executor.py"
      via: "OpenCodeSession 的 live/history 标记驱动 ensure 分支"
      pattern: "backend_session_id|live|history|load_session"
    - from: "core/executor.py"
      to: "tests/core/test_executor_acp.py"
      via: "session/load vs session/new vs 直接 prompt 的调用序列"
      pattern: "session/load|session/new|session/prompt"
---

<objective>
修正 ACP 会话 ensure 逻辑，把“当前连接里已 live 的 session 续用”和“仅保存了历史 session ID 的恢复”拆成两条语义清晰的路径。

Purpose: 当前 gap 的根因是所有已有 `backend_session_id` 都被当成必须先 `session/load` 的历史恢复，导致连续 `/oc` 会误建新 session，也掩盖了失效历史绑定的真实恢复语义。
Output: 明确的 sender 会话状态模型 + `core/executor.py` 中按 live/history 分流的 ensure 逻辑 + 对应回归测试。
</objective>

<execution_context>
@/home/LiHaoyuan/workspace/cool_stuff/astrbot_plugin_opencode/.opencode/get-shit-done/workflows/execute-plan.md
@/home/LiHaoyuan/workspace/cool_stuff/astrbot_plugin_opencode/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-会话内核与生命周期统一/02-UAT.md
@.planning/phases/02-会话内核与生命周期统一/02-01-SUMMARY.md
@core/session.py
@core/executor.py
@tests/core/test_executor_acp.py
@tests/core/test_session_state.py

<interfaces>
From `core/session.py`:
```python
class OpenCodeSession:
    work_dir: str
    backend_session_id: Optional[str]

    def drop_backend_session(self):
        ...

    def bind_backend_session(self, session_id: str):
        ...

    def reset_live_session(self):
        ...

````

From `core/executor.py`:
```python
async def ensure_session(self, session: OpenCodeSession) -> ExecutionResult
async def load_session(self, session: OpenCodeSession) -> ExecutionResult
async def _ensure_session_ready(
    self,
    session: OpenCodeSession,
    allow_recreate_after_load_failure: bool = True,
) -> SessionEnsureResult
````

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先把 live 会话续用 / 历史恢复分流写成回归测试</name>
  <files>tests/core/test_executor_acp.py, tests/core/test_session_state.py</files>
  <behavior>
    - Test 1: 已有 `backend_session_id` 且 sender 标记为当前 ACP 连接内 live 时，连续 `run_prompt()` 必须直接复用该 session；即使 `loadSession=False`，调用序列也只能是 `initialize -> session/prompt`，不能出现 `session/load` 或 `session/new`。
    - Test 2: 仅有历史绑定（非 live）的 `backend_session_id` 时，`ensure_session()` 仍应尝试恢复；恢复失败后普通 `/oc` 路径会 drop 坏绑定并创建新 session，`session_recovery_failed=True`。
    - Test 3: 显式 `load_session()` 在历史恢复失败时必须 fail-closed，返回 `acp_load_session_failed`，并让 `backend_session_id` 与 live runtime 状态一起清空。
    - Test 4: `OpenCodeSession` 的状态辅助方法要能区分“绑定历史 session 但尚未 live”和“当前连接已 live”，避免一个布尔判断混淆两种状态。
  </behavior>
  <action>先改 `tests/core/test_executor_acp.py` 和 `tests/core/test_session_state.py`，把 UAT 中暴露的问题钉死：删除/改写当前把 `loadSession=False` 时新建 session 视为正确行为的旧断言，新增“live 会话续用不依赖 `loadSession`”与“坏历史绑定恢复失败后才重建”的调用序列断言。测试里不要只断言最终 `session_id`，还要精确断言 client 调用顺序与 `session_recovery_failed` 标记，避免再次把语义回归成“能跑就行”。</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/core/test_executor_acp.py tests/core/test_session_state.py -q</automated>
  </verify>
  <done>测试已经明确区分 live 续用、历史恢复、显式加载失败三条路径；当前错误实现会因调用序列或状态断言不符而红灯。</done>
</task>

<task type="auto">
  <name>Task 2: 在 session/executor 中落地 live 与 history 的双态语义</name>
  <files>core/session.py, core/executor.py</files>
  <action>在 `core/session.py` 为 `OpenCodeSession` 增加显式的 backend live/ready 状态表达，保持它仍是 per-sender 状态唯一真源；`bind_backend_session()` 只表示“记住一个待恢复的历史 session ID”，不要把它直接当成当前连接已 live，成功 `session/new` / `session/load` / 首次有效 runtime hydrate 后再把该绑定标记为 live。随后在 `core/executor.py` 重写 `_ensure_session_ready()`：1) 若已有 `backend_session_id` 且该绑定已 live，则直接返回可用，不再依赖 `loadSession`，从而修复连续 `/oc` 新建 session 的问题；2) 若只是历史绑定，则仅在恢复路径上尝试 `session/load`；3) 历史恢复失败时，普通 `ensure_session()` 先 `drop_backend_session()` 再 `session/new`，而显式 `load_session()` 继续 fail-closed 返回错误；4) 保持恢复失败时清空 `pending_permission`、`prompt_running` 等 live runtime 派生状态。不要把这套状态拆到 `main.py` 或模块级缓存里，遵守 `core/session.py` 作为状态真源的边界。</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/core/test_executor_acp.py tests/core/test_session_state.py -q</automated>
  </verify>
  <done>连续 `/oc` 会续用同一 live backend session；只有历史绑定恢复失败时才会清掉坏绑定并重建；显式历史加载失败仍保持 fail-closed。</done>
</task>

</tasks>

<verification>
- `.venv/bin/python -m pytest tests/core/test_executor_acp.py tests/core/test_session_state.py -q`
- 连续 `/oc` 场景对应测试里 client 调用序列应为 `initialize -> session/prompt`，不出现 `session/new`
</verification>

<success_criteria>

- 连续 `/oc` 不再因为 `loadSession=False` 而新建 ACP session
- sender 状态能区分 live 会话续用与历史会话恢复
- 坏历史绑定在 `/oc` 路径上会清掉并恢复到可继续执行的新会话
  </success_criteria>

<output>
After completion, create `.planning/phases/02-会话内核与生命周期统一/02-03-SUMMARY.md`
</output>
