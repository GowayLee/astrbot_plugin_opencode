{
  "basic_config": {
    "description": "基础配置",
    "type": "object",
    "items": {
      "only_admin": {
        "description": "仅限管理员使用",
        "type": "bool",
        "default": true,
        "hint": "开启后，只有 AstrBot 的管理员可以使用此插件功能"
      },
      "acp_command": {
        "description": "ACP 启动命令",
        "type": "string",
        "default": "opencode",
        "hint": "用于启动 ACP 后端的可执行命令，默认使用环境变量中的 opencode。"
      },
      "acp_args": {
        "description": "ACP 启动参数",
        "type": "list",
        "default": ["acp"],
        "hint": "启动 ACP 后端时追加的命令参数，默认等价于执行 opencode acp。"
      },
      "acp_startup_timeout": {
        "description": "ACP 启动超时(秒)",
        "type": "int",
        "default": 30,
        "hint": "等待 ACP 后端完成启动与握手的最长时间。"
      },
      "work_dir": {
        "description": "默认工作目录",
        "type": "string",
        "default": "",
        "hint": "OpenCode 启动时的默认目录。留空则默认为插件数据目录下的 workspace"
      },
      "proxy_url": {
        "description": "代理地址",
        "type": "string",
        "default": "",
        "hint": "OpenCode 运行时的网络代理，例如 http://127.0.0.1:7890 。留空则不使用代理。"
      },
      "allow_file_writes": {
        "description": "允许文件写入操作",
        "type": "bool",
        "default": true,
        "hint": "关闭后，涉及创建、修改、删除文件的请求会在预检查阶段直接拒绝。"
      },
      "confirm_timeout": {
        "description": "敏感操作确认超时(秒)",
        "type": "int",
        "default": 30,
        "hint": "等待用户确认敏感操作的超时时间，默认 30 秒。"
      }
    }
  }
}
